# Data Commons MCP Server Configuration
# 
# This file contains all available configuration options for the Data Commons MCP server.
# To use this configuration:
# 1. Copy this file to .env: cp .env.sample .env
# 2. Edit .env with your actual values
# 3. Uncomment and set the variables you need
#
# Environment variables take precedence over .env file values.
# All DC_* variables are optional except DC_API_KEY and DC_TYPE.

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# API key for Data Commons (required for all configurations)
DC_API_KEY=your_datacommons_api_key_here

# Type of Data Commons to use (optional, defaults to "base")
# Options: "base" (production Data Commons) or "custom" (custom instance)
DC_TYPE=base

# =============================================================================
# CUSTOM DC CONFIGURATION (required when # DC_TYPE=custom)
# =============================================================================

# Base URL for custom Data Commons instance (required when # DC_TYPE=custom)
# Example: https://staging-datacommons-web-service-650536812276.northamerica-northeast1.run.app
# CUSTOM_DC_URL=https://staging-datacommons-web-service-650536812276.northamerica-northeast1.run.app

# =============================================================================
# COMMONLY USED OPTIONAL CONFIGURATION
# =============================================================================

# Comma-separated list of root topic DCIDs (optional, custom DC only)
# Most commonly used for custom DCs to define available topics
# Example: ONE/topic/BasicHealth,ONE/topic/Economy
# DC_ROOT_TOPIC_DCIDS=ONE/topic/BasicHealth

# Search scope for queries (optional, custom DC only)
# Controls search behavior for custom DCs (base DC always uses "base_only")
# Options: "base_only", "custom_only", "base_and_custom"
# Default: "base_and_custom" for custom DC
# DC_SEARCH_SCOPE=base_and_custom

# =============================================================================
# NON-PROD ROOTS (optional, base DC only)
# =============================================================================

# Use these variables to run the server against non-prod (autopush, staging) or local instances
# of the Data Commons API and Search endpoints.
# When using a local instance, you may also need to use the 
# --skip-api-key-validation command-line flag if running without a DC_API_KEY.

# Root URL for a non-prod or local Data Commons API (mixer) instance
# DC_API_ROOT=http://localhost:8081/v2

# Root URL for a non-prod or local Data Commons Search (website) instance
# DC_SEARCH_ROOT=http://localhost:8080

# Root URL for Data Commons API key validation
# Configure for non-prod environments
# DC_API_KEY_VALIDATION_ROOT=https://api.datacommons.org
